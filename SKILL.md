---
name: local-trajectory-recall
description: >-
  跨会话检索豆包本地落盘的对话历史（trajectory.jsonl），把"之前讨论过、决定过、查过但当前上下文里已经没有"的原话找回来。
  用于：(1) 会话变长或重启后早期细节被压缩/丢失，想不起"当时怎么说的、定了什么参数、为什么选这个方案"；
  (2) 用户说"上次/之前/我们之前讨论过那个…""之前那个项目/方案/数据"，但当前上下文没有，要去历史会话里翻；
  (3) 想知道本机历史上都开过哪些对话、某段历史里查过什么/决定过什么；
  (4) 为记忆机制、交接、复盘提供原始依据。只检索本机已有记录，不联网、不改任何对话。
  不要用于：需要从外部网页/新信息找答案的任务（那是搜索），也不要把 trajectory 内容当结论直接转述——它是原始流水账。
compatibility: "豆包专属能力，且仅 macOS 实测可用。它依赖豆包桌面端把会话逐字落盘到本机 .sessions/.../trajectory.jsonl；换别的 AI 助手（Claude/GPT 等）或非豆包客户端根本没有这份数据，本技能对它们无意义。Windows/Linux 未适配。执行前先 uname -s 判平台，非 macOS 即停并告知。"
---

# 本地对话轨迹回溯（trajectory recall）

## 平台适用（执行前先读）

- **这是豆包专属能力**：只在豆包桌面端（本机 macOS）有意义。它查的是豆包自己落盘的 trajectory.jsonl——**别的 AI 助手/平台既没有这份文件、数据格式也不同，装了也跑不出东西**。
- **实测平台**：macOS（Darwin）。Windows/Linux 未适配；动手前先 `uname -s`，非 Darwin 就停下告知"该平台需先适配"，不要想当然换等价命令硬跑。
- 本技能只检索本机已有记录、不联网、不改任何对话；脱离豆包桌面端环境即无用。

## 它是什么

豆包桌面端把每次会话的逐字记录按 session 落盘在本机：
`~/Library/Application Support/Doubao/<Profile>/.doubao/agent_mode/workspace/.sessions/<sid>/agents/<aid>/system/trajectory.jsonl`

每行一个 JSON，`role` ∈ user/assistant/tool，`content` 为内容。
一个 `sid` 下可能有多个 `aid`：`m_` 开头=主对话（用户可见），`s_`/`o_` 开头=子 agent（工具调用，无用户对话，脚本已自动跳过）。

这是**本机逐字全量记录**，比"上下文里的压缩摘要"更原始、更全，但也含系统提示/工具返回/token。

## 隐私红线（必须遵守）

- trajectory 含系统提示、工具返回、token、密钥等敏感内容——**只本机自用**。
- **绝不**把 trajectory 原文粘进 git、项目文档、对外聊天或任何会外传的产物。
- 回复用户时只转述你需要的那一小段原话要点，不要整段倾倒、不要带 token。
- 它是原始流水账，不是整理过的结论；要下结论仍以项目文档（HANDOFF/需求/ADR）为准。

## 怎么用

统一入口：`python3 scripts/recall.py <命令>`（系统 python3，无第三方依赖）。

| 命令 | 干什么 |
|---|---|
| `list` | 列出最近主会话（时间倒序，每条约多少轮、首条用户消息）——先看有哪些历史会话可翻 |
| `where` | 当前正在用的是哪条 trajectory（多会话时确认找对了） |
| `stats` | 当前会话规模（轮数/角色/字数） |
| `stats --id <sid片段>` | 指定某会话的规模 |
| `search <关键词>` | 在**当前会话**搜用户+助手原话 |
| `search --all <关键词>` | **跨全部主会话**搜（找"之前讨论过的东西"） |
| `search --id <sid片段> <关键词>` | 只在指定会话里搜 |
| `export out.md` | 导出某会话可读对话（跳过 tool） |

`--id` 用 sid 的后几位片段即可（list 里显示的 `…159106`）。

## 典型工作流

1. **想不起之前说过什么** → 先 `list` 看有哪些会话 → 锁定相关会话（按首条用户消息/时间）→ `search --id <片段> <关键词>` 或直接 `search --all <关键词>`。
2. **当前会话被压缩、某参数记不清** → 直接 `search <关键词>`（就在当前会话里）。
3. **跨项目/跨天回忆** → `search --all <关键词>`，命中会标注 `[…sid]`，再按 sid 深挖。

## 注意

- 命中太多就换更独特的关键词（用项目名、参数名、术语，别用"那个""之前"）。
- 跨会话搜索会扫全部主会话，量大时稍慢，正常。
- 多会话并行时，先用 `where` / `list` 确认当前会话，别误读成别的会话。
